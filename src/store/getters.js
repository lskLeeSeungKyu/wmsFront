export const getters = {
    grid_inbOrder(state) {
        return state.grid_inbOrder;
    },

    grid_outOrder(state) {
        return state.grid_outOrder;
    },

    grid_mstUser(state) {
        return state.grid_mstUser;
    },

    grid_mainPage(state) {
        return state.grid_mainPage;
    },

    grid_inbEntry(state) {
        return state.grid_inbEntry;
    },

    grid_outEntry(state) {
        return state.grid_outEntry;
    },

    grid_inbEntryDetail(state) {
        return state.grid_inbEntryDetail;
    },

    grid_outEntryDetail(state) {
        return state.grid_outEntryDetail;
    },

    grid_inbInspection(state) {
        return state.grid_inbInspection;
    },

    grid_outInspection(state) {
        return state.grid_outInspection;
    },

    grid_inbInspectionDetail(state) {
        return state.grid_inbInspectionDetail;
    },

    grid_outInspectionDetail(state) {
        return state.grid_outInspectionDetail;
    },
    
    grid_stock(state) {
        return state.grid_stock;
    },

    isLogin(state) {
        return state.isLogin;
    },

    isLoading(state) {
        return state.isLoading;
    },

    newMessageText(state) {
        return state.newMessageText;
    },

    btnShow(state) {
        return state.btnShow;
    },

    userModal(state) {
        return state.userModal;
    },

    uploadModal(state) {
        return state.uploadModal;
    },
    
}